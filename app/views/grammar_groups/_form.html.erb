<%= form_for(@grammar_group) do |f| %>
  <% if @grammar_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grammar_group.errors.count, "error") %> prohibited this grammar_group from being saved:</h2>

      <ul>
      <% @grammar_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequence_number %><br>
    <%= f.text_field :sequence_number %>
  </div>
  <div class="field">
    <%= f.label :group_type %><br>
    <%= f.text_field :group_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
