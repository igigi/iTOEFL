<%= form_for(@add_question) do |f| %>
  <% if @add_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_question.errors.count, "error") %> prohibited this add_question from being saved:</h2>

      <ul>
      <% @add_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :mark_id %><br>
    <%= f.text_field :mark_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
