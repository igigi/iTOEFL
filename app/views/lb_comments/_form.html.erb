<%= form_for(@lb_comment) do |f| %>
  <% if @lb_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lb_comment.errors.count, "error") %> prohibited this lb_comment from being saved:</h2>

      <ul>
      <% @lb_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :live_broadcast_id %><br>
    <%= f.text_field :live_broadcast_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
