<%= form_for(@broadcast_set) do |f| %>
  <% if @broadcast_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@broadcast_set.errors.count, "error") %> prohibited this broadcast_set from being saved:</h2>

      <ul>
      <% @broadcast_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :cover %><br>
    <%= f.text_field :cover %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
